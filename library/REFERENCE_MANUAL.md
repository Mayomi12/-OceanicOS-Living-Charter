# The OceanicOS Ω∞ Reference Manual

Architecture, runtime, APIs.

---

The living reference for the running system is generated from the system itself and cannot drift from the code:

- **[Generated documentation](../core/docs.html)** — every released capability and the live API contract (`api.describe()`), build 0018.
- **[Continuous Verification](../core/verify-all.html)** — the whole project's single green light.
- **[Build Log](../BUILD_LOG.md)** — every verified capability, recorded.

This volume gathers architecture and runtime reference material as it grows.
